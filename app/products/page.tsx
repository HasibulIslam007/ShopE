import { ProductCard } from "@/components/ProductCard"; import { categories, products } from "@/lib/products";
export default async function ProductsPage({searchParams}:{searchParams:Promise<{category?:string}>}){const params=await searchParams; const selected=params.category||"All pieces"; const shown=selected==="All pieces"?products:products.filter(p=>p.category===selected); return <main className="mx-auto max-w-7xl px-6 pb-24 pt-12 md:px-10"><div className="mb-12 max-w-2xl"><p className="text-xs font-bold uppercase tracking-[.25em] text-coral">The collection</p><h1 className="mt-3 text-6xl font-black tracking-[-.07em]">Find your<br/><i className="font-normal">everyday.</i></h1><p className="mt-5 text-ink/60">Objects with a point of view, made to live well with you.</p></div><div className="mb-10 flex flex-wrap gap-2">{categories.map(category=><a key={category} href={category==="All pieces"?"/products":`/products?category=${category}`} className={`rounded-full border px-5 py-2 text-sm ${selected===category?"border-ink bg-ink text-white":"border-ink/20 hover:border-ink"}`}>{category}</a>)}</div><div className="grid grid-cols-2 gap-x-4 gap-y-12 md:grid-cols-3 md:gap-8">{shown.map(p=><ProductCard key={p.id} product={p}/>)}</div></main>}