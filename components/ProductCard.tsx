"use client";
import Image from "next/image"; import Link from "next/link"; import { Product } from "@/lib/products"; import { useCart } from "@/context/CartContext";
export function ProductCard({product}:{product:Product}){const {add}=useCart(); return <article className="group"><Link href={`/products/${product.id}`}><div className="relative aspect-[4/5] overflow-hidden rounded-[2rem]" style={{backgroundColor:product.color}}>{product.isNew&&<span className="absolute left-4 top-4 z-10 rounded-full bg-white px-3 py-1 text-xs font-bold">New in</span>}<Image src={product.image} alt={product.name} fill className="object-cover transition duration-500 group-hover:scale-105" sizes="(max-width: 768px) 50vw, 25vw" /></div></Link><div className="flex items-start justify-between gap-3 px-1 pt-4"><div><p className="text-sm text-ink/55">{product.category}</p><Link href={`/products/${product.id}`} className="font-semibold">{product.name}</Link></div><div className="text-right"><p className="font-semibold">${product.price}</p><button onClick={()=>add(product)} className="mt-2 text-xs font-bold underline underline-offset-4">Add to bag</button></div></div></article>}